config:
  partial_parse: true
  printer_width: 120
  send_anonymous_usage_stats: false
  use_colors: true
transform:
  outputs:
    prod:
      dbname: postgres
      host: localhost
      pass: password
      port: 2000
      schema: public
      threads: 8
      type: postgres
      user: postgres
  target: prod
